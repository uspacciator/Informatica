/*
	Name: voti compiti
	Copyright: colamonico
	Author: mercurio gianluca
	Date: 13/12/22 12:31
	Description: Dato in input un vettore di N numeri reali corrispondenti ai voti del
compito di informatica di una classe, dare in output: il numero di voti
sufficienti, il numero di voti insufficienti ed una variabile booleana esito
che assuma valore V se i voti sufficienti sono di più di quelli insufficienti,
valore F altrimenti.
*/
#include <iostream>     
using namespace std;
int main()              
{
const int MAX=100;	
int vett[MAX];
int totvoti;
int votisuf;
int votinsuf;


cout<<"Inserisci il numero dei compiti in classe di informatica: "<<endl;
cin>>vett[MAX];

cout<<"I voti sufficienti del compito di informatica sono: "<<endl;
if(totvoti>=6);{
cout<<totvoti<<endl;
}
cout<<"I voti sufficienti del compito di informatica sono: "<<endl;
if(totvoti<=5);{
cout<<totvoti<<endl;
}


















return 0;
}

